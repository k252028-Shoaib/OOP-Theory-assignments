As you can tell by the name, this is a miniature car marketplace system with no gui (terminal based) in C++. A user can make accounts, listings and access them. A user can also send messages to other accounts. All the accounts, listings, and messages are stored in a vector database (simply an array) which is saved in a file. The data is auto saved when the program is exited and auto loaded when the program starts.
